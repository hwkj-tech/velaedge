# Runtime identity and non-secret startup settings live in EDGE_ID.toml.
# Keep only secrets and process-level logging controls in this 0600 file.
EDGEOPS_EDGE_TOKEN=CHANGE_ME_ONE_TIME_EDGE_TOKEN
RUST_LOG=edge_runtime=info
